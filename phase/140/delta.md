140 declares nothing: a highlight group's id was its index + 1, recovered from
a hash key by subtracting the key's offset; the lookup scans the array for the
same unique upper-cased name.  The check's probes see a lookup in another
case, a group a failed :hi took back, and a link.
