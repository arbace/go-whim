130 declares nothing: the three tests for (pos_T *)-1 were never taken -- nothing
in this tree returns it -- and fold away keeping the branch that runs.  The
probe of every way the three sites are reached ('a, `a, :'a, g;) and its
unset-mark controls are the check's.
