139 declares nothing: the table searches are musl's search on typed pointers,
probe for probe, and :undolist's sort orders strings by strcmp() as qsort did
-- strings that compare equal print the same.  The check's five probes see
each search and the sort.
