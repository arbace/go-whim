133 declares nothing: whim has one buffer, so buflist_findnr() answers "the
current buffer, if its number is nr" and the buffer hash table goes.  The
check's mark probes (m[ m] m" and their jumps) are what setmark_pos() reaches.
