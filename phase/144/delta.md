144 declares nothing: Insert mode reaches the same code without jumping into
a case.  The check probes every key whose case jumped that the pty can
deliver; CTRL-C (a signal there), stop_insert_mode's site and a non-CTRL-C
interrupt character's are shown by where their continue and break bind.
