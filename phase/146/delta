# 146 declares nothing: a memline node is a header naming its block rather than
# the block's first member, so no node is cast to a block.  The memline corpus
# and the check's split-and-undo probe see the tree.
